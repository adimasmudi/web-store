'use client';

import { Navbar } from '@/components/navbar/Navbar';

export const CartPage = () => {
  return (
    <div>
      <Navbar type="user" />
      <p>This is cart page</p>
    </div>
  );
};
